---
all:
  children:
    webservers:
      hosts:
        <server_name>:
          ansible_host: "{{ host }}"
          ansible_user: "{{ user }}"
          ansible_ssh_private_key_file: "{{ ssh_key }}"
